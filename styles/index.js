import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  primaryColor: {
    color: "#2C6BED",
  },
  authButton: {
    width: 300,
    marginTop: 10,
  },
});

export default styles;
