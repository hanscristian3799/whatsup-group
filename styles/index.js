import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  primaryColor: {
    color: "#00CC66",
  },
  authButton: {
    width: 300,
    marginTop: 10,
  },
});

export default styles;
